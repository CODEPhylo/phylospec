Alignment observed = nexus("hallo.nex")

Tree tree ~ Coalescent(populationSize=1.0*observed.ntax, taxa=observed.taxa)
QMatrix q = JC69()

@observedAs(observed)
Alignment alignment ~ PhyloCTMC(tree=tree, Q=q)