Vector<Real> vector1 = [
    log
    (
        x = exp(
                        5.0
            ),
       base =
            10


    )
]
Vector<Real> vector2 = [          10           ]


// EXPECT AST
// (AS Vector<Real> vector1 (AR (CA log (AA x (CA exp (AA null 5.0))) (AA base 10))))
// (AS Vector<Real> vector2 (AR 10))
// EXPECT AST

// EXPECT TYPE_ERRORS
// EXPECT TYPE_ERRORS

// EXPECT TYPES
// Vector<Real; num=1>
// Vector<Real<;value=10>; num=1>
// EXPECT TYPES