Integer a = 100 * 50 + 15
PositiveInteger a = 100 * 50 + 15 + -1

// EXPECT AST
// (AS Integer a (+ (* 100 50) 15))
// (AS PositiveInteger a (+ (+ (* 100 50) 15) (- 1)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// Expression of type `Integer | Real` cannot be assigned to variable `a` of type `PositiveInteger`. Change the type of the variable to `Integer | Real`, or change what you assign to it.
// EXPECT TYPE_ERRORS