// indexing a non-collection type is an error
String name = "Alice"
Real x = name[0]

Boolean flag = true
Real y = flag[1]

// EXPECT AST
// (AS String name "Alice")
// (AS Real x (IX name 0))
// (AS Boolean flag true)
// (AS Real y (IX flag 1))
// EXPECT AST

// EXPECT TYPE_ERRORS
// This element cannot be accessed with an index. You use the index notation ('items[3]') on an element which is neither a vector, a matrix nor a map. Remove the index.
// This element cannot be accessed with an index. You use the index notation ('items[3]') on an element which is neither a vector, a matrix nor a map. Remove the index.
// EXPECT TYPE_ERRORS
