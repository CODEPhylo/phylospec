Vector<PositiveReal> vector = [
    10.0,
    2.0,
    5.0
]

String x = "Hallo"

PositiveReal vector2[x] = vector[x] * vector[x] for x in 1:num(vector)

// EXPECT AST
// (AS Vector<PositiveReal> vector (AR 10.0 2.0 5.0))
// (AS String x "Hallo")
// (IS (AS PositiveReal vector2 (* (IX vector x) (IX vector x))) x (R 1 (CA num (AA vector vector))))
// EXPECT AST

// EXPECT TYPE_ERRORS
// EXPECT TYPE_ERRORS