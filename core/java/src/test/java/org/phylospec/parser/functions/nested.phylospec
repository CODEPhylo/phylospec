Real y ~ Normal(mean=log(100), sd=2.0)
PositiveReal z ~ LogNormal(
    logMean=exp(log(100)), logSd=exp(x=y)
)

// EXPECT AST
// (DR Real y (CA Normal (AA mean (CA log (AA null 100))) (AA sd 2.0)))
// (DR PositiveReal z (CA LogNormal (AA logMean (CA exp (AA null (CA log (AA null 100))))) (AA logSd (CA exp (AA x y)))))
// EXPECT AST

// EXPECT TYPE_ERRORS
// EXPECT TYPE_ERRORS