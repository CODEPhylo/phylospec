Real y ~ Normal(mean=log(100), sd=2.0)
PositiveReal z ~ LogNormal(
    meanlog=exp(log(100)), sdlog=exp(x=y)
)

// EXPECT AST
// (DR Real y (CA Normal (AA mean (CA log (AA null 100))) (AA sd 2.0)))
// (DR PositiveReal z (CA LogNormal (AA meanlog (CA exp (AA null (CA log (AA null 100))))) (AA sdlog (CA exp (AA x y)))))
// EXPECT AST