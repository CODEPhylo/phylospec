Vector<Real> vector = [
    nchar
    (
        a = 10,
        b =
            ntaxa(
                20
            ),


    )
]
Vector<Real> vector = [                     ]


// EXPECT AST
// (AS Vector<Real> vector (AR (CA abs (AA a 10) (AA b (CA min (AA null 20))))))
// (AS Vector<Real> vector (AR ))
// EXPECT AST