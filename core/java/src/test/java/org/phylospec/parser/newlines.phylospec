Vector<Real> vector = [
    someFunc
    (
        a = 10,
        b =
            someOtherFunc(
                20
            ),


    )
]
Vector<Real> vector = [                     ]


// EXPECT
// (AS Vector<Real> vector (AR (CA someFunc (AA a 10) (AA b (CA someOtherFunc (AA null 20))))))
// (AS Vector<Real> vector (AR ))
