Vector<Real> vector = [
    log
    (
        x = log(
                        5.0
            ),
       base =
            10


    )
]
Vector<Real> vector = [                     ]


// EXPECT AST
// (AS Vector<Real> vector (AR (CA log (AA x (CA log (AA null 5.0))) (AA base 10))))
// (AS Vector<Real> vector (AR ))
// EXPECT AST