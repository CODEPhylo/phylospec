Vector<Real> vector = [
    nchar
    (
        a = 10,
        b =
            ntaxa(
                20
            ),


    )
]
Vector<Real> vector = [                     ]


// EXPECT AST
// (AS Vector<Real> vector (AR (CA nchar (AA a 10) (AA b (CA ntaxa (AA null 20))))))
// (AS Vector<Real> vector (AR ))
// EXPECT AST