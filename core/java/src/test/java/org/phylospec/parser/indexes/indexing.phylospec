Vector<PositiveReal> vector = [
    10.0,
    2.0,
    5.0
]

PositiveReal x = vector[2]

Matrix<PositiveReal> matrix = [
   [10.0, 2.0],
   [10.0, 2.0],
   [10.0, 5.0],
]

PositiveReal y = matrix[2, 1]
PositiveReal z = matrix[vector[2], 1 * 100]

// EXPECT AST
// (AS Vector<PositiveReal> vector (AR 10.0 2.0 5.0))
// (AS PositiveReal x (IX vector 2))
// (AS Matrix<PositiveReal> matrix (AR (AR 10.0 2.0) (AR 10.0 2.0) (AR 10.0 5.0)))
// (AS PositiveReal y (IX matrix 2 1))
// (AS PositiveReal z (IX matrix (IX vector 2) (* 1 100)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// EXPECT TYPE_ERRORS