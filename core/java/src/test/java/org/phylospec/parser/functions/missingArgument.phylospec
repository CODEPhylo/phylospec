Real a ~ Beta(alpha = 10)

// EXPECT AST
// (DR Real a (CA Beta (AA alpha 10)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// Function `Beta` with the given arguments is not known:
// Function `Beta` takes the required argument `beta`
// Function `Beta` takes the required argument `beta`
// EXPECT TYPE_ERRORS