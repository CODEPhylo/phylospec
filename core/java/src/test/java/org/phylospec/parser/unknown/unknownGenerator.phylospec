Integer tree = 20
Real b = CCD0(tree)

// EXPECT AST
// (AS Integer tree 20)
// (AS Real b (CA CCD0 (AA null tree)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// The function `CCD0` does not exist. Are you looking for `k80'?
// EXPECT TYPE_ERRORS