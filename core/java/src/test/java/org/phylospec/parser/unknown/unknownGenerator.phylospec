Integer tree = 20
Real b = CCD0(tree)

// EXPECT AST
// (AS Integer tree 20)
// (AS Real b (CA CCD0 (AA tree tree)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// Function `CCD0` is not known
// EXPECT TYPE_ERRORS