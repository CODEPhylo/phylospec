PositiveReal logSd = exp(10)
Real x ~ LogNormal(3, logSd)
Real y ~ LogNormal(x, logSd)
Real z ~ LogNormal(logSd, x)
QMatrix b = hky(10)

// EXPECT AST
// (AS PositiveReal logSd (CA exp (AA null 10)))
// (DR Real x (CA LogNormal (AA null 3) (AA null logSd)))
// (DR Real y (CA LogNormal (AA null x) (AA null logSd)))
// (DR Real z (CA LogNormal (AA null logSd) (AA null x)))
// (AS QMatrix b (CA hky (AA null 10)))
// EXPECT AST

// EXPECT TYPE_ERRORS
// Function `LogNormal` with the given arguments does not exist. There are 2 different versions of 'LogNormal'. They cannot be used due to the following reasons:
//
// 1. Function `LogNormal` takes no argument named `x`. Do you mean 'logSd'?
// 2. Function `LogNormal` takes no argument named `x`. Do you mean 'mean'?
//
// Fix any of these issues to use 'LogNormal'.
// Function `LogNormal` with the given arguments does not exist. There are 2 different versions of 'LogNormal'. They cannot be used due to the following reasons:
//
// 1. Function `LogNormal` takes no argument named `x`. Do you mean 'logSd'?
// 2. Function `LogNormal` takes no argument named `x`. Do you mean 'mean'?
//
// Fix any of these issues to use 'LogNormal'.
// Function `hky` takes the required argument `baseFrequencies`.
// EXPECT TYPE_ERRORS