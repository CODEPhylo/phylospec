package shared;

import org.phylospec.annotations.AdapterLibrary;

/** Precompiled adapter index used to model metadata supplied by a dependency JAR. */
@AdapterLibrary(ClasspathFrequenciesAdapter.class)
public final class ClasspathAdapterIndex {

    private ClasspathAdapterIndex() {}
}
