Alignment alignment = fromNexus("primate-mtDNA.nex")
Taxa taxa = taxa(alignment)
Tree tree ~ Yule(
    birthRate=1.0, taxa
)
Distribution<Vector<Rate>> dist = RelaxedClock(
    clockRate=10,
    base=LogNormal(mean=1.0, logSd=2.0),
    tree
)

// EXPECTED_TILES
// TILING_SUCCESS
// EXPECTED_TILES