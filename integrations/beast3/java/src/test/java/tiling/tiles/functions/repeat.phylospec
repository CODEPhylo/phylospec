Vector<Integer> repeatInt = repeat(
    value=10, num=5
) // right now, this will be parsed as "10" could be both int or real
Vector<Real> repeatReal = repeat(
    value=10.5, num=3
)

// EXPECTED_TILES
// (StateNodeAssignmentTile (RepeatRealTile (value (LiteralTile)) (num (LiteralTile))))
// (StateNodeAssignmentTile (RepeatRealTile (value (LiteralTile)) (num (LiteralTile))))
// EXPECTED_TILES