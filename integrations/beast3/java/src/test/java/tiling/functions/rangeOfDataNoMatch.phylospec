Alignment data = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Tree tree ~ Yule(birthRate=1.0, taxa=taxa(data))

Integer numBranches = numBranches(tree)

Vector<Integer> range = range(0, end=numBranches)

// EXPECTED_TILES
// Unsupported operation. BEAST 2.8 cannot deal with the value you provided for the 'start' argument for 'range'.
// EXPECTED_TILES

// EXPECTED BEAST STATE
// NO_STATE
// EXPECTED BEAST STATE