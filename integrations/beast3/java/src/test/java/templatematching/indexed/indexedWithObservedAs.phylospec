// TEMPLATE
Real x[i] ~ $dist observed as $data[i] for i in 1:$n
// TEMPLATE

// QUERY
Vector<Real> observations = readData()
Real x[i] ~ Normal(mean=0.0, sd=1.0) observed as observations[i] for i in 1:10
// QUERY

// MATCH
// $n -> 10
// $dist -> (CA Normal (AA mean 0.0) (AA sd 1.0))
// $data -> (CA readData )
// MATCH
