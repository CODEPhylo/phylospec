Alignment data = fromNexus(
    file="src/test/java/resources/comparison/H1N1pdm_2009.nex",
    date=parse(regex=".*_(\d+(?:\.\d+)?)$")
)

Taxa taxa = taxa(data)

PositiveReal populationSize = 0.5

Tree tree ~ Coalescent(
    populationSize=constantPopulationFunction(
        populationSize=populationSize
    ),
    taxa=taxa
)

mcmc {
    Integer chainLength = 100000000
    Integer randomSeed = 1234

    Logger fileLogger = fileLogger(
        logEvery=1000,
        file="target/comparison-diagnostics/beastx-constant-coalescent-prior-only.log",
        parameters=[
            "posterior",
            "prior",
            "likelihood",
            "tree_prior",
            "tree.height",
            "tree.treeLength"
        ]
    )

    Logger treeLogger = treeLogger(
        logEvery=1000,
        file="target/comparison-diagnostics/beastx-constant-coalescent-prior-only.trees",
        trees=[tree]
    )
}
