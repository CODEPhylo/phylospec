QMatrix q = hky(
    kappa~LogNormal(logMean=1.0, logSd=0.5),
    baseFrequencies~Dirichlet(concentration=repeat(1.0, num=4))
)

// EXPECTED_TILES
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED BEASTX STATE
// SN: kappa, baseFrequencies
// P: kappa_prior, baseFrequencies_prior
// EXPECTED BEASTX STATE