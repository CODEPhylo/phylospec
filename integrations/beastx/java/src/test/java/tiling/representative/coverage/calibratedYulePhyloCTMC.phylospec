Alignment data = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Taxa taxa = taxa(data)

Rate birthRate ~ LogNormal(logMean=0.0, logSd=1.0)

Tree tree ~ Yule(
    birthRate=birthRate,
    taxa=taxa
)

Age calibratedRoot = rootAge(tree=tree) observed between [1.0, 10.0]

QMatrix qMatrix = jc69()

Alignment alignment ~ PhyloCTMC(
    tree=tree,
    qMatrix=qMatrix,
    branchRates~StrictClock(clockRate=1.0, tree=tree)
) observed as data

// EXPECTED_TILES
// TILING_SUCCESS
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED BEASTX STATE
// SN: birthRate
// P: birthRate_prior
// TM: tree
// TP: tree_prior
// CP: rootCalibration
// L: alignment_likelihood
// EXPECTED BEASTX STATE