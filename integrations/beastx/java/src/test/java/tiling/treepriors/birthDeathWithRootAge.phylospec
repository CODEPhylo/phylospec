Real rootAge ~ LogNormal(mean=1.0, logSd=1.0)
Alignment data = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Taxa taxa = taxa(data)
Tree tree ~ BirthDeath(
    diversificationRate=1.0,
    turnover=0.5,
    rootAge=rootAge,
    taxa=taxa
)

// EXPECTED_TILES
// TILING_SUCCESS
// EXPECTED_TILES

// EXPECTED BEASTX STATE
// SN: rootAge
// TM: tree
// P: rootAge_prior
// TP: tree_prior
// EXPECTED BEASTX STATE