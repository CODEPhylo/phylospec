Alignment data = fromNexus("src/test/java/resources/primate-mtDNA.nex")
Taxa taxa = taxa(data)
Tree tree ~ Yule(birthRate=1.0, taxa=taxa)

Vector<Rate> branchRates ~ RelaxedClock(
    clockRate=1.0,
    base=LogNormal(mean=1.0, logSd=0.0),
    tree=tree
)

// EXPECTED_TILES
// Unsupported operation. BEAST 2.8 cannot deal with the value you provided for the 'logSd' argument for 'LogNormal'.
// EXPECTED_TILES

// EXPECTED BEASTX STATE
// NO_STATE
// EXPECTED BEASTX STATE