Alignment data = fromNexus(
    file="${REPOSITORY_ROOT}/validation/examples/testProteinModels/protein.nex"
)

Taxa taxa = taxa(data)

QMatrix qMatrix = jtt()

Tree tree ~ Yule(
    birthRate=1.0,
    taxa=taxa
)

Alignment alignment ~ PhyloCTMC(
    tree=tree,
    qMatrix=qMatrix
) observed as data

mcmc {
    Integer chainLength = 5000000
    Integer randomSeed = 1234
}
