# BEAST 2 source

The source example for this controlled validation case is:

```text
/Applications/BEAST 2.7.7/examples/testSliceHKY.xml
```

The distributed source is not duplicated here because its 768-site alignment
is identical to the canonical alignment already retained by
`validation/examples/testHKY/testHKY.nex`.

`beast2-aligned.xml` is the executable comparison reference. The semantic
differences from the distributed source are documented in the parent
`README.md`.
